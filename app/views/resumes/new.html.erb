<div class="container">    
   <%= form_for @resume, html: { multipart: true } do |f| %>   
   <% if @resume.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resume.errors.count, "error") %> prohibited this file from being upload</h2>
    </div>
    <% end %>
      <%= f.label :name %>   
      <%= f.text_field :name %>   
      <br><br>   
      <%= f.label :attachment %>   
      <%= f.file_field :attachment %>   
      <br>   
      <%= f.submit "Save" %>   
   <% end %> 
   </div>



